# Инструкция по настройке и запуску бота

## 1. БЫСТРЫЙ СТАРТ (Локальное тестирование)

### Шаг 1: Установить зависимости

```bash
pip install -r requirements.txt
```

### Шаг 2: Настроить бота в канале

1. Откройте ваш канал **ENGUERRADOS** (ID: `-1003586810622`)
2. Добавьте бота как администратора
3. Дайте права:
   - ✅ **Invite users via link**
   - ✅ **Ban users**

### Шаг 3: Локальный запуск (БЕЗ webhook)

Для начального тестирования можно запустить только бота:

```bash
python bot.py
```

Откройте бота в Telegram и проверьте команды:
- `/start` - должно показать приветствие
- Кнопка "Comprar suscripción" - покажет планы

**ВАЖНО:** Без webhook платежи работать не будут! Это только для проверки интерфейса.

---

## 2. ПОЛНЫЙ ЗАПУСК (С webhook для платежей)

### Вариант A: Локальное тестирование с ngrok

#### 1. Установить ngrok

Скачайте с https://ngrok.com/download

#### 2. Запустить ngrok

```bash
ngrok http 8080
```

Вы получите URL вида: `https://abc123.ngrok-free.app`

#### 3. Обновить WEBHOOK_URL

Откройте файл с переменными окружения и измените:

```env
WEBHOOK_URL=https://abc123.ngrok-free.app/webhook
```

**ВАЖНО:** При каждом перезапуске ngrok URL меняется!

#### 4. Настроить Stripe Webhook

1. Зайдите в Stripe Dashboard: https://dashboard.stripe.com/test/webhooks
2. Нажмите "Add endpoint"
3. URL: `https://abc123.ngrok-free.app/webhook`
4. Выберите события:
   - `checkout.session.completed`
   - `invoice.paid`
   - `invoice.payment_failed`
   - `customer.subscription.deleted`
   - `customer.subscription.updated`
5. Нажмите "Add endpoint"
6. Скопируйте **Signing secret** (начинается с `whsec_...`)
7. Добавьте в переменные окружения:

```env
STRIPE_WEBHOOK_SECRET=whsec_your_secret_here
```

#### 5. Запустить бота и webhook

**Windows:**
```bash
start.bat
```

**Linux/Mac или Python скрипт:**
```bash
python run.py
```

**Или запустить отдельно:**

Терминал 1:
```bash
python bot.py
```

Терминал 2:
```bash
python webhook_server.py
```

---

### Вариант B: Деплой на сервер (VPS)

Для продакшена нужен сервер с публичным IP и доменом.

#### Популярные варианты:

1. **Railway** (рекомендуется, простой деплой)
   - https://railway.app/
   - Поддержка Python
   - Автоматический HTTPS

2. **Heroku**
   - https://www.heroku.com/
   - Нужен Procfile

3. **VPS** (DigitalOcean, Linode, Vultr)
   - Полный контроль
   - Нужна настройка Nginx + SSL

#### Пример для VPS:

1. **Установить на сервере:**
```bash
# Клонировать код
git clone your-repo.git
cd your-repo

# Установить зависимости
pip3 install -r requirements.txt

# Настроить переменные окружения
nano .env  # или создать файл
```

2. **Запустить в фоне:**
```bash
nohup python3 bot.py > bot.log 2>&1 &
nohup python3 webhook_server.py > webhook.log 2>&1 &
```

3. **Настроить автозапуск (systemd):**

Создать файл `/etc/systemd/system/enguerrados-bot.service`:
```ini
[Unit]
Description=ENGUERRADOS Telegram Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 /path/to/bot/bot.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Создать файл `/etc/systemd/system/enguerrados-webhook.service`:
```ini
[Unit]
Description=ENGUERRADOS Webhook Server
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/bot
ExecStart=/usr/bin/python3 /path/to/bot/webhook_server.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Включить и запустить:
```bash
sudo systemctl enable enguerrados-bot enguerrados-webhook
sudo systemctl start enguerrados-bot enguerrados-webhook
sudo systemctl status enguerrados-bot enguerrados-webhook
```

---

## 3. НАСТРОЙКА КРОН-ЗАДАЧИ

Для автоматической проверки истёкших подписок:

```bash
crontab -e
```

Добавить строку (проверка каждые 6 часов):
```cron
0 */6 * * * cd /path/to/bot && python3 check_subscriptions.py >> cron.log 2>&1
```

---

## 4. ТЕСТИРОВАНИЕ ПЛАТЕЖЕЙ

### Тестовые карты Stripe:

**Успешная оплата:**
- Номер: `4242 4242 4242 4242`
- Дата: Любая будущая (например: 12/34)
- CVC: Любые 3 цифры (например: 123)
- Почтовый индекс: Любой

**Провал оплаты:**
- Номер: `4000 0000 0000 0002`

**3D Secure:**
- Номер: `4000 0027 6000 3184`

### Процесс тестирования:

1. Откройте бота в Telegram
2. Нажмите `/start`
3. Нажмите "Comprar suscripción"
4. Выберите план (например, "1 mes")
5. Нажмите "Pagar ahora"
6. Откроется Stripe Checkout
7. Введите тестовую карту `4242 4242 4242 4242`
8. Нажмите "Subscribe"
9. Вернитесь в Telegram
10. Вы должны получить сообщение с инвайт-ссылкой

---

## 5. МОНИТОРИНГ И ОТЛАДКА

### Проверить статус сервисов:

```bash
# Webhook сервер
curl http://localhost:8080/health

# Должен вернуть:
# {"status":"ok","timestamp":"2026-01-22T..."}
```

### Просмотр логов:

```bash
# Логи бота
tail -f bot.log

# Логи webhook
tail -f webhook.log

# Логи крон-задачи
tail -f cron.log
```

### Проверка БД:

```bash
sqlite3 bot_database.db

# В SQLite консоли:
.tables                    # Список таблиц
SELECT * FROM users;       # Все пользователи
SELECT * FROM subscriptions WHERE status='active';  # Активные подписки
.exit
```

### Админские команды в боте:

- `/stats` - Статистика (доступна только админам с ID из ADMIN_IDS)

---

## 6. ЧАСТО ВСТРЕЧАЮЩИЕСЯ ПРОБЛЕМЫ

### Бот не отвечает:

1. Проверить, запущен ли процесс:
```bash
ps aux | grep bot.py
```

2. Проверить токен бота:
```bash
# Тест API
curl https://api.telegram.org/bot7884349748:AAEZC82Nd72L1eR1rhupuDWihjWdEKG4bd8/getMe
```

### Webhook не получает события:

1. Проверить URL в Stripe Dashboard
2. Проверить, что webhook сервер запущен
3. Проверить логи: `tail -f webhook.log`
4. Проверить firewall (порт 8080 должен быть открыт)

### Пользователь не получил доступ:

1. Проверить в БД наличие подписки
2. Проверить права бота в канале
3. Проверить логи webhook
4. Вручную создать инвайт-ссылку:
```python
python -c "
from telegram import Bot
import asyncio
bot = Bot('YOUR_TOKEN')
link = asyncio.run(bot.create_chat_invite_link(-1003586810622, member_limit=1))
print(link.invite_link)
"
```

---

## 7. БЕЗОПАСНОСТЬ

### Важные моменты:

1. **НЕ коммитить .env в Git!** (уже в .gitignore)
2. Использовать **HTTPS** для webhook в продакшене
3. Настроить **STRIPE_WEBHOOK_SECRET** для проверки подписи
4. Регулярно проверять логи на подозрительную активность
5. Ограничить доступ к серверу (firewall, SSH keys)

---

## 8. ПОДДЕРЖКА

Если возникли проблемы:

1. Проверьте логи
2. Проверьте конфигурацию в `.env`
3. Проверьте права бота в канале
4. Проверьте настройки webhook в Stripe

---

## СТРУКТУРА ФАЙЛОВ

```
tgBOT333igorkapa/
├── bot.py                     # Telegram бот (основной процесс)
├── webhook_server.py          # Webhook сервер для Stripe
├── stripe_integration.py      # Модуль работы с Stripe API
├── database.py                # Модуль работы с БД
├── config.py                  # Конфигурация
├── check_subscriptions.py     # Скрипт проверки подписок (крон)
├── run.py                     # Единый запуск всех сервисов
├── start.bat                  # Запуск для Windows
├── requirements.txt           # Python зависимости
├── README.md                  # Документация
├── SETUP_INSTRUCTIONS.md      # Эта инструкция
├── .env                       # Переменные окружения (НЕ коммитить!)
├── .gitignore                 # Игнорируемые файлы
└── bot_database.db           # База данных (создаётся автоматически)
```

---

**Готово! Бот настроен и готов к работе! 🚀**
